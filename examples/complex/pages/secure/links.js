import { AppLink } from 'smart-react-routing';

const links = {
  access: new AppLink('/access'),
  superSettings: new AppLink('/super_settings'),
  admin: new AppLink('/admin'),
};

export default links;
